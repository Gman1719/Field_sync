# Archived Legacy Prototypes

This directory contains early prototype iterations (`client` and `server`) preserved for historical reference.

- **Active Frontend**: [`/src`](../src) (React 18 + Tailwind CSS + Dexie IndexedDB + PWA)
- **Active Backend**: [`/api`](../api) (Express + PostgreSQL)
